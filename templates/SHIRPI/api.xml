<?xml version="1.0" encoding="utf-8"?>
<shirpi version="0.1">
	{% for restaurant in results %}
	<location name="{{ restaurant.name }}" address="{{restaurant.address}}" city="{{restaurant.location.city}}" province="{{restaurant.location.province}}" rha="{{restaurant.location.rha}}">
		{% ifequal display_type "full" %}
		loop through reports and output
		<report date="{{report.date}}"  type="{{report.type}}" priority="{{report.priority}}"> 
			{% for report in reports %}
				{% for item in report.items.all %}}
					<item>{{item.number}}</item>
				{% endfor %}
			{% endfor %}
		</report>
		{% endifequal %}
	</location>
	{% endfor %}
</shirpi>

